# สรุปไฟล์ที่แก้ไข (สำหรับ deploy รอบนี้)

ไฟล์ ZIP นี้รวมทุกการแก้ไขตั้งแต่ต้นบทสนทนา — deploy ทับของเดิมได้เลย ไม่ต้องนำการแก้ไขรอบก่อนมาไล่ใส่เอง

## แก้ไขล่าสุด (รอบนี้)
- **แบนเนอร์โปรโมชั่นหน้าหลักไม่โดนตัดขอบอีกแล้ว** — รอบก่อนหน้าบังคับความสูงคงที่ + ครอปภาพ (`object-fit: cover`) ทำให้ภาพที่แอดมินอัปโหลดโดนตัดขอบ แก้กลับเป็นแสดงภาพเต็มตามสัดส่วนจริง (`height: auto`) เสมอ ไม่มีการตัดขอบอีกไม่ว่าภาพจะมีสัดส่วนแบบไหน
- อัปเดตเวอร์ชันไฟล์เป็น `v=20260808h`

## สำคัญมาก — เรื่อง CDN Cache (Vercel)
ทุกครั้งที่ deploy โค้ดใหม่ ต้องเข้าไป **Purge Cache ที่ Vercel Dashboard → CDN → Caches → Purge Cache → เลือก "All content" + "CDN, ISR, and Image Cache" → กด Purge** ไม่งั้นจะยังเห็นเวอร์ชันเก่าอยู่

## รวมทุกการแก้ไขจากรอบก่อนหน้า

### ดีไซน์ / เลย์เอาต์
- แบนเนอร์โปรโมชั่นหน้าหลัก: แสดงเต็มภาพ ไม่ครอป (รอบนี้)
- CTA banner: ไล่สีมุมชนมุม (`to bottom right`) + ปุ่มสีส้ม
- หน้าโปรไฟล์ + หน้ากระเป๋าเงิน: เอาการ์ด VIP badge สีดำออก, สีไล่ม่วง-ชมพู
- เอาปุ่มกล้องออกจากหน้าหลัก, โลโก้ช่องทางชำระเงิน/ขนส่งใหญ่ขึ้น 3 คอลัมน์คงที่
- พื้นที่ว่างด้านล่างแก้ครบทุกหน้า, กล่องข้อความหลังบ้านเต็มจอ, เอาปุ่มโทร/วิดีโอคอลออก
- แจ้งเตือน (toast) ขึ้นจากด้านบนแบบ SMS
- Cache-busting ที่ CSS/JS ทั้งฝั่งลูกค้าและแอดมิน

### รหัสแนะนำ
- เป็นตัวเลข 5 หลัก ทุกจุดที่ออกรหัส + migration แปลงบัญชีเดิมทั้งหมด

### ระบบแจ้งเตือน
- Push Notification จริงที่เครื่อง (แม้ปิดแอป) + เสียง + สั่น ทุก event ในระบบ
- Admin → ตั้งค่า → การแจ้งเตือน: แก้ไขข้อความ + เปิด/ปิดเป็นหมวดหมู่ได้จริง
- ข้อความแจ้งเตือน "ปรับโดยเจ้าหน้าที่" (เปลี่ยนจาก "ปรับโดยแอดมิน")

### ทีมงาน/สิทธิ์การใช้งาน + บริหารการเงิน
- เพิ่ม/แก้ไข/ลบทีมงาน กำหนดสิทธิ์เป็นรายหน้าได้จริง มีบัญชี "เจ้าของร้าน" คุมสิทธิ์
- หน้า "บริหารการเงิน" จัดการยอดเงินลูกค้าแบบรวมศูนย์ + โปรแกรมแนะนำเพื่อน
- ปุ่มแช่แข็งบัญชี = ล้างยอดเงินเป็น 0 + เปลี่ยนสถานะเป็น "ดำเนินการถอนเงิน"

## Backend
- `backend/db.js` — ตาราง `notifications`, คอลัมน์ permissions/is_owner/notif_seen_at/account_status, migration รหัสแนะนำ 5 หลัก
- `backend/lib/notify.js` — จุดกลางแจ้งเตือน (event+template) พร้อม push จริง
- `backend/routes/team.js`, `notifications.js` — จัดการทีมงาน + feed แจ้งเตือน
- `backend/routes/customers.js`, `tickets.js`, `orders.js`, `bank-account.js`, `auth.js`, `social-auth.js` — ผูกจุดแจ้งเตือน, รหัสแนะนำ 5 หลัก, ปุ่มแช่แข็ง

## Admin panel (`admin/`)
ทีมงาน/สิทธิ์, การแจ้งเตือน, บริหารการเงิน, กระดิ่งแจ้งเตือนทั่วไป, กล่องข้อความเต็มจอ, cache-busting

## Customer app (`customer/`)
Push+เสียงแจ้งเตือน (ขึ้นจากด้านบนแบบ SMS), หน้าแจ้งเตือนรวม, ดีไซน์หน้าโปรไฟล์/กระเป๋าเงิน/หน้าหลักตามภาพอ้างอิง, พื้นที่ว่างด้านล่างแก้ครบทุกหน้า, cache-busting

## ค้างการตัดสินใจ
- **ฟอนต์ทั้งระบบ**: รอลูกค้ายืนยันฟอนต์ที่ต้องการจาก 6 ตัวที่ระบบรองรับ (ตั้งค่าได้เองที่ Admin → ตั้งค่า → Branding)

## ขั้นตอน deploy (สำคัญ — ทำครบทุกข้อ)
1. Deploy `backend/` ก่อน แล้ว**รีสตาร์ทเซิร์ฟเวอร์** (migration รันอัตโนมัติ)
2. ตั้งค่า `VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `VAPID_SUBJECT` ใน Render ถ้ายังไม่ได้ตั้ง
3. Deploy `admin/` และ `customer/` บน Vercel
4. **Purge Cache ที่ Vercel** (CDN → Caches → Purge Cache → All content → Purge)
5. ล้างแคช/ปิดเปิดแอปที่เครื่องอีกครั้งก่อนตรวจสอบผล
