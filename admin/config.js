// config.js — edit these two values when you deploy the backend.
// In local development the defaults below point at `npm start` running on port 4000.
window.APP_CONFIG = {
  API_BASE: 'http://localhost:4000/api',
  SOCKET_URL: 'http://localhost:4000',
};
